// Kotlin has the following conditionals
// 1. Use of if to specify a block of codes to be executed if the condition is true
// 2. Use of else to specify if the condition above is false
// 3. else if to add new conditions to our test
// 4. when to specify many alterations to be executed
var a = 48
var b = 34
fun main(){
    if(a > b){
        println("$a is greater than $b")
    }
    else {
        println("$a is not graeter than $b")
    }
}